import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.wirzeigenherz',
  appName: 'Wir zeigen Herz',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
