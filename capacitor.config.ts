import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'wir-zeigen-herz',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
